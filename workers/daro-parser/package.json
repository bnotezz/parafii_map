{
    "name": "daro-parser-worker",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "deploy": "wrangler deploy index.js --config ./wrangler.toml",
        "test": "node --experimental-vm-modules node_modules/.bin/jest"
    },
    "dependencies": {
        "@cloudflare/puppeteer": "^0.0.5"
    },
    "devDependencies": {
        "jest": "^29.6.2",
        "jest-environment-jsdom": "^29.6.2",
        "jest-fetch-mock": "^3.0.3",
        "wrangler": "^4.15.2"
    }
}
